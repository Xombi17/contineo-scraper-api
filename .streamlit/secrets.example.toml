# Example Streamlit Secrets Configuration
# Copy this to secrets.toml and fill in your actual values
# NEVER commit secrets.toml to Git!

# Primary Database (Prisma Postgres)
DIRECT_URL = "postgresql://username:password@host.region.provider.tech/database?sslmode=require"

# Optional: Secondary Database (Neon)
DATABASE_URL = "postgresql://username:password@host.region.provider.tech/database?sslmode=require"

# Example URLs (replace with your actual values):
# DIRECT_URL = "postgresql://neondb_owner:abc123xyz@ep-cool-cloud-123456.us-east-1.aws.neon.tech/neondb?sslmode=require"
# DATABASE_URL = "postgresql://user:pass@ep-spring-voice-a1yre8if-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"

# How to get your database URL:
# 
# For Prisma Postgres:
# 1. Go to https://console.prisma.io
# 2. Select your project
# 3. Go to Settings → Connection strings
# 4. Copy the "Direct URL" (not pooled)
#
# For Neon:
# 1. Go to https://console.neon.tech
# 2. Select your project
# 3. Go to Connection Details
# 4. Copy the connection string
# 5. Make sure it includes ?sslmode=require
